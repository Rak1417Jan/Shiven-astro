/**
 * Re-export subscriber nav config so @/data/nav resolves.
 * Source of truth: @/data-subscriber/nav
 */
export {
  subscriberNavItems,
  type NavItem,
  type NavItemChild,
} from "@/data-subscriber/nav";
