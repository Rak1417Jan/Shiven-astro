/** Key used in localStorage for auth (customer or subscriber). */
export const AUTH_STORAGE_KEY = "astro-customer-auth";

/** Base path for all customer panel routes. Use for links and redirects. */
export const CUSTOMER_BASE = "/customer";

/** Base path for all subscriber panel routes. */
export const SUBSCRIBER_BASE = "/subscriber";
